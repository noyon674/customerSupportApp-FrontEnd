import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ext4solutions.app",
  appName: "customer-service-app-front",
  webDir: "build",
};

export default config;
